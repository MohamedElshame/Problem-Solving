#include <iostream>
using namespace std;
// find maximum of 10 only using 2 variables
int main() {
    int a,temp;
    temp=0;
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }

    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cin>>a;
    if (a>temp) {
        temp=a;
    }
    cout<<temp<<endl;
}


